extends layout

block content
    //- div(class='metaReview')
    //-     li(class="imgReviewEdit"): img(src=boardGame.image class='boardGameImageEdit' )
    //-     a(href=linkToGame class='reviewName')= boardGame.title

    include includes/ind-boardgame-info

        //- NTS: insert category if doing categories
        form(action=`/boardgames/${boardGame.id}/reviews/new` method="post" class='f')
            input(type="hidden" name="_csrf" value=csrfToken)
            label(for="comment" class="wdyt") What did you think?
            textarea(placeholder="Enter your review" name="comment" class="inputValueEdit")
            button(type="submit" class="updateButtonEdit") Post
